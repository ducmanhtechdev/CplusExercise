#include <bits/stdc++.h>

using namespace std;

int main()
{
    // Exercise19
    int a, b, c;
    cin >> a >> b >> c;

    cout << (a / 2) + (a % 2) + (b / 2) + (b % 2) + (c / 2) + (c % 2);

    return 0;
}