#include <bits/stdc++.h>

using namespace std;

int main()
{ // Exercise28
    double toan, van, anh;
    cin >> toan >> van >> anh;
    cout << fixed << setprecision(1) << (toan * 2 + van * 2 + anh) / 5.0;

    return 0;
}